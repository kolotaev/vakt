import pytest

from vakt.checker import AttributesChecker
from vakt.policy import Policy


# @pytest.mark.skip
# # @pytest.mark.parametrize('policy, field, what, result', [
# # ])
# def test_fits(policy, field, what, result):
#     c = AttributesChecker()
#     assert result == c.fits(policy, field, what)
